<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      No INTERNET permission, on purpose and permanently.

      The app makes no network calls at runtime (docs/00-big-picture.md). Leaving the
      permission out means that decision is enforced by the platform rather than by
      review: anything that tries to fetch at runtime fails loudly in development.
      Network access belongs to tools/dataset-pipeline, which runs on a desktop.
    -->

    <application
        android:name=".PokedexApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Pokedex">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          WorkManager starts on demand with Hilt's worker factory (PokedexApplication), so
          its default startup initializer is removed. The provider itself stays: the
          profile installer uses it too.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>
</manifest>
