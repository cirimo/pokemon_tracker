<?xml version="1.0" encoding="utf-8"?>
<!--
  Cloud backup is off (android:allowBackup="false") because durability here is
  user-driven export/import, not a silent platform backup we cannot inspect or
  version. See docs/adr/0007-backup-format.md.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
    </device-transfer>
</data-extraction-rules>
